import { Modal, Cascader } from 'antd';
import styled from 'styled-components';

export const AccessProfileModal  = styled(Modal)`
  .ant-modal-body {
    padding: 50px;
  }
`;


export const CascaderComponent = styled(Cascader)`
  margin-top: 20px;
`;